# 📐 AWS EC2 Deployment Architecture

Visual guide to understand how your application is deployed.

---

## Architecture Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                         INTERNET                                 │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         │ HTTPS/HTTP
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                    AWS SECURITY GROUP                            │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  Port 22 (SSH)    - Your IP Only                         │  │
│  │  Port 80 (HTTP)   - Open to Internet (0.0.0.0/0)        │  │
│  │  Port 443 (HTTPS) - Open to Internet (0.0.0.0/0)        │  │
│  └──────────────────────────────────────────────────────────┘  │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                    EC2 INSTANCE                                  │
│                  (Ubuntu 22.04 LTS)                              │
│                                                                  │
│  ┌────────────────────────────────────────────────────────┐   │
│  │                    UFW FIREWALL                         │   │
│  │  - Allow SSH (22)                                       │   │
│  │  - Allow Nginx Full (80, 443)                          │   │
│  └────────────────────────────────────────────────────────┘   │
│                         │                                        │
│                         ▼                                        │
│  ┌────────────────────────────────────────────────────────┐   │
│  │                    NGINX                                │   │
│  │              (Reverse Proxy)                            │   │
│  │                                                         │   │
│  │  - Listens on Port 80/443                              │   │
│  │  - SSL Termination (if configured)                     │   │
│  │  - Forwards to localhost:3000                          │   │
│  └────────────────┬───────────────────────────────────────┘   │
│                   │                                             │
│                   ▼                                             │
│  ┌────────────────────────────────────────────────────────┐   │
│  │                    PM2                                  │   │
│  │           (Process Manager)                             │   │
│  │                                                         │   │
│  │  - Keeps app running 24/7                              │   │
│  │  - Auto-restart on crash                               │   │
│  │  - Starts on system boot                               │   │
│  └────────────────┬───────────────────────────────────────┘   │
│                   │                                             │
│                   ▼                                             │
│  ┌────────────────────────────────────────────────────────┐   │
│  │              NEXT.JS APP                                │   │
│  │        (AI Homecare Management)                         │   │
│  │                                                         │   │
│  │  - Runs on localhost:3000                              │   │
│  │  - Server-side rendering                               │   │
│  │  - API routes                                          │   │
│  │  - Authentication                                      │   │
│  └────────────────┬───────────────────────────────────────┘   │
│                   │                                             │
└───────────────────┼─────────────────────────────────────────────┘
                    │
                    │ External APIs
                    │
        ┌───────────┴───────────┬──────────────┬──────────────┐
        │                       │              │              │
        ▼                       ▼              ▼              ▼
┌──────────────┐    ┌──────────────┐  ┌──────────┐  ┌──────────┐
│   SUPABASE   │    │   STRIPE     │  │  OPENAI  │  │   n8n    │
│              │    │              │  │          │  │          │
│ - Database   │    │ - Payments   │  │ - AI     │  │ - Auto   │
│ - Auth       │    │ - Webhooks   │  │ - Scoring│  │ - mation │
│ - Storage    │    │ - Billing    │  │          │  │          │
└──────────────┘    └──────────────┘  └──────────┘  └──────────┘
```

---

## Request Flow

### 1. User Visits Website

```
User Browser
    │
    │ https://yourdomain.com
    ▼
AWS Security Group (Port 443 open)
    │
    ▼
EC2 Instance
    │
    ▼
UFW Firewall (Nginx Full allowed)
    │
    ▼
Nginx (Port 443)
    │
    │ SSL Termination
    │
    │ Proxy to localhost:3000
    ▼
PM2 (Manages Next.js)
    │
    ▼
Next.js App (Port 3000)
    │
    │ Renders Page
    ▼
Response sent back to user
```

---

## File Structure on EC2

```
/home/ubuntu/ai-homecare/
├── app/                    # Next.js pages
├── components/             # React components
├── lib/                    # Utility libraries
├── public/                 # Static assets
├── .next/                  # Build output
├── node_modules/           # Dependencies
├── .env.local             # Environment variables (SECURE)
├── package.json           # Dependencies list
└── next.config.js         # Next.js config

/etc/nginx/
├── sites-available/
│   └── ai-homecare        # Nginx config
└── sites-enabled/
    └── ai-homecare        # Symlink to config

/etc/letsencrypt/           # SSL certificates (if using)
└── live/
    └── yourdomain.com/
        ├── fullchain.pem
        └── privkey.pem

/var/log/nginx/
├── access.log             # Nginx access logs
└── error.log              # Nginx error logs

~/.pm2/logs/
├── ai-homecare-out.log    # PM2 stdout logs
└── ai-homecare-error.log  # PM2 error logs
```

---

## Data Flow

### Patient Creation Flow

```
User clicks "New Patient Registration"
    │
    ▼
React Component (new-patient-modal.tsx)
    │
    │ Collects form data
    │ Validates with Zod
    ▼
POST /api/patients
    │
    ▼
API Route (app/api/patients/route.ts)
    │
    │ Validates request
    │ Checks authentication
    │ Validates with Zod
    ▼
Supabase Client
    │
    │ RLS checks organization
    │ Inserts patient record
    ▼
Database (PostgreSQL)
    │
    ▼
Creates Audit Log
    │
    ▼
Returns patient data
    │
    ▼
Updates UI
```

---

## AI Risk Assessment Flow

```
API Call: POST /api/ai/risk-assessment
    │
    ▼
Fetches patient data from Supabase
    │
    ▼
Fetches recent incidents & missed visits
    │
    ▼
Calls OpenAI API
    │
    │ Sends patient data
    │ Requests risk assessment
    ▼
OpenAI GPT-4 analyzes data
    │
    ▼
Returns risk score & recommendations
    │
    │ riskScore: 0-100
    │ riskLevel: low/medium/high/critical
    │ factors: array of risk factors
    │ recommendations: array of actions
    ▼
Updates patient record in Supabase
    │
    ▼
Creates audit log
    │
    ▼
Returns assessment to client
```

---

## Security Layers

```
┌─────────────────────────────────────────────────┐
│  Layer 1: AWS Security Group                    │
│  - Controls network access                      │
│  - Restricts SSH to your IP                    │
└───────────────────┬─────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────────────┐
│  Layer 2: UFW Firewall                          │
│  - OS-level firewall                            │
│  - Only allows necessary ports                  │
└───────────────────┬─────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────────────┐
│  Layer 3: Nginx                                 │
│  - SSL/TLS encryption                           │
│  - Request filtering                            │
│  - DDoS protection                              │
└───────────────────┬─────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────────────┐
│  Layer 4: Next.js Middleware                    │
│  - Authentication checks                        │
│  - Session validation                           │
│  - Subscription enforcement                     │
└───────────────────┬─────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────────────┐
│  Layer 5: API Route Validation                  │
│  - Zod input validation                         │
│  - Role-based access control                    │
│  - Organization scoping                         │
└───────────────────┬─────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────────────┐
│  Layer 6: Supabase RLS                          │
│  - Row-level security                           │
│  - Multi-tenant isolation                       │
│  - Database-level protection                    │
└─────────────────────────────────────────────────┘
```

---

## High Availability Setup (Optional Future Enhancement)

```
                    ┌─────────────────┐
                    │  Load Balancer  │
                    │     (ALB)       │
                    └────────┬────────┘
                             │
                ┌────────────┴────────────┐
                │                         │
                ▼                         ▼
        ┌──────────────┐        ┌──────────────┐
        │  EC2 Instance│        │  EC2 Instance│
        │  (Primary)   │        │  (Standby)   │
        └──────────────┘        └──────────────┘
                │                         │
                └────────────┬────────────┘
                             │
                             ▼
                    ┌─────────────────┐
                    │    Supabase     │
                    │   (Managed DB)  │
                    └─────────────────┘
```

---

## Monitoring Dashboard (Conceptual)

```
┌────────────────────────────────────────────────────┐
│            CloudWatch Dashboard                     │
├────────────────────────────────────────────────────┤
│                                                     │
│  CPU Usage:         ████████░░ 80%                 │
│  Memory Usage:      ████████░░ 75%                 │
│  Disk Usage:        ███░░░░░░░ 30%                 │
│  Network In:        1.2 MB/s                       │
│  Network Out:       800 KB/s                       │
│                                                     │
│  Application Status: ✅ Running                    │
│  Nginx Status:       ✅ Active                     │
│  PM2 Status:         ✅ Online                     │
│                                                     │
│  Active Users:       147                           │
│  Requests/min:       542                           │
│  Avg Response Time:  120ms                         │
│                                                     │
└────────────────────────────────────────────────────┘
```

---

## Cost Breakdown (Approximate Monthly)

```
┌─────────────────────────────────────────┐
│  AWS EC2 t3.medium                      │
│  2 vCPU, 4 GB RAM                       │
│  $30-40/month                           │
├─────────────────────────────────────────┤
│  EBS Storage (30 GB)                    │
│  $3/month                               │
├─────────────────────────────────────────┤
│  Data Transfer                          │
│  First 1 GB free, then $0.09/GB        │
│  ~$5-10/month (varies)                  │
├─────────────────────────────────────────┤
│  Elastic IP (optional)                  │
│  Free if attached, $3.65/month idle    │
├─────────────────────────────────────────┤
│  TOTAL                                  │
│  ~$40-55/month                          │
└─────────────────────────────────────────┘

External Services (Not included):
- Supabase: Free tier or $25/month
- OpenAI: Pay-as-you-go (~$10-50/month)
- Stripe: 2.9% + $0.30 per transaction
```

---

This architecture provides a solid foundation for a production application with room to scale as your user base grows!
